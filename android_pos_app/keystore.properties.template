# Copy this file to keystore.properties and update with your values
# DO NOT commit keystore.properties to version control!

# Key store file path (relative to project root)
storeFile=pos-release-key.keystore

# Key store password
storePassword=YOUR_KEYSTORE_PASSWORD

# Key alias
keyAlias=pos-release-key

# Key password
keyPassword=YOUR_KEY_PASSWORD

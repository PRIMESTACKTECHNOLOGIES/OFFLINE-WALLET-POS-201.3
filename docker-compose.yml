version: '3.8'

services:
  pos-app:
    build: .
    ports:
      - "7000:7000"
    volumes:
      - ./data:/app/data
    environment:
      - PORT=7000
      - DATABASE_URL=./data/pos_offline.sqlite
    restart: unless-stopped
